# D04 Overview evidence (implementation gate, not human approval)

Worker-recorded browser evidence for District #27 (D04). Human visual approval of the combined Overview/Flows design is a separate D05/D09 checkpoint.

Setup: worktree console served by `uv run python -m district dashboard --no-open --port 8790` from `.factory/wt-27`, reading the installed host registry (five real factories, engine `factory-3de0a24`). Headless Chromium 1440×1000 unless stated. All times UTC, 2026-09-08. Real-telemetry captures are the source-of-truth fleet; the fixture case is labeled separately.

## Real telemetry

- `overview-desktop.webp` (00:13:40Z, `/api/fleet` revision 20): `mikeroySoft/district` occupies **dispatcher** and **worker** (#27, this very ticket) concurrently while **ticket** is `unknown`; capacity `1 of 2 active`; latest transition `enter worker · #27 · 23:58:57Z`; every card equally weighted, slug order. Count strip `0 normal · 0 attention · 5 unknown`, matching each card's `assessment`. Runtime source `partial` (producer `systemctl/service:transitioning`, `descendants_not_scanned`) so active stages are shown solid without pulse — pulse requires a fresh confirmed active execution.
- `fleet-district-snapshot.json` (00:22:30Z, revision 30): the projected `/api/fleet` entry (events tail only) behind the screenshots, with source timestamps, `activity.dispatcher` (capacity, `latest_transition`), history window and gaps.
- Real observed transition/completion: at 00:11:22Z `mikeroySoft/gpuflo` dispatched on its timer. The browser recorded `arrive` on dispatcher/scheduling/triage/landing and `complete` on triage/landing at 00:11:27.355Z, once; identical polls afterwards produced no further motion (`motions` count unchanged over the next minute). Rail rows: `00:11:23Z entered dispatcher`, `00:11:23Z exited (completed) triage — no_tickets`, `00:11:25Z exited (completed) scheduling`.
- Worker → gate-check → gate without conflation: the `factory gate` runs of this ticket (00:20:30Z and 00:21:15Z) appeared as new `gate-check`/`gate` executions with `arrive`/`complete` motion while the `worker #27` execution stayed `stage-active` in its own node; rail rows `00:21:26Z exited (completed) gate #27`, `entered gate-check #27`.
- Selection into Flows: clicking the `worker` stage of `mikeroySoft/district` navigated to `/?view=flows&factory=mikeroySoft%2Fdistrict&stage=worker` (heading Flows, scope label and `stage: worker` selection shown, one card). `history.back()` returned to Overview with scroll restored to 420 px and zero replayed motion.
- Watch mode (screenshot in transcript, 00:15Z): `nav`, scope select, intro and footer hidden; names, status, diagrams, facts, findings remain; URL unchanged; note “Watch mode: secondary controls hidden (Esc exits)”; Esc exits. Persisted via `sessionStorage` only.
- Pause animation: `data-motion="paused"`, computed `animation-play-state: paused` on stage nodes, note “Animation paused in this browser only; factories and facts keep updating.” Facts kept updating (revision advanced).
- Hidden tab (simulated `visibilitychange` with `document.hidden=true`): `data-hidden` set, play-state paused; on return, no motion replayed, revision had advanced, countdown refreshed.
- Reduced motion (`prefers-reduced-motion: reduce` emulated): computed `animation-name: none` on active stages, note “Reduced motion: system preference disables animation; facts still update.”, all 9 facts still rendered.
- Keyboard: Tab order `Watch mode → Pause animation → factory name → each stage link in diagram order`; focus stayed on `merge-eligibility` across polls (revision 60 → 95) and across the reduced-motion/pause changes.
- Narrow viewport 390×844: cards single column, stages stacked (`flex-direction: column`), no horizontal overflow, focus ring retained.
- Stalled source: the worktree server was stopped at 00:15:50Z. Within two polls the page showed the notice “Cached fleet observation is currently unavailable; last known state is shown.”, `data-connection="lost"`, no `.pulse` animation, cards and facts unchanged (last known state). After restart the page reconnected without any motion; a browser-stamped gap row is added when the outage exceeds 60 s (deterministic check: `state.lastPollAt` set 120 s back → rail row “No fleet observation in this browser between 00:15:20Z and 00:17:20Z; events from that interval are baselined, not replayed.”). Injected duplicate/unseen events on a reconnect poll were baselined, not animated.
- Prolonged watch-mode session: started 00:19:24Z; checked at 00:21:34Z (2 min, 25 distinct revisions, 0 failed polls, DOM node count constant at 493, focus and scroll retained). See the final line of this file for the closing check.

## Fixture-only fault cases (NOT real telemetry)

`fixture-fault-cases.webp` and `fixture-fleet.json` come from a throwaway server feeding `health.classify` + `dashboard_read.safe_fleet` with synthetic sources, to show distinctions the installed fleet did not exhibit at the time: `fixture/running` (fresh active worker+gate pulse, capacity wait on review, `2 of 2 active`, countdown), `fixture/scheduled` (scheduled waiting, known wait), `fixture/capped` (warning finding, “no automatic dispatch (capped)”), `fixture/stopped` (`scheduling.unexpected_stop` error finding), `fixture/stale` (muted card, stationary `stage-active · stale` last known state, “runtime stale, 15m old”), `fixture/shared-dependency` (shared-scope `dependency.unavailable` counted in the strip, `runtime.mechanism_unavailable`, held resource with unknown owner). Strip: `2 normal · 3 attention · 1 unknown · 1 shared-scope finding`.

Closing check of the prolonged watch-mode session: 00:19:24Z → 00:23:51Z (4.5 min headless, 53 distinct cache revisions, one worktree-server restart reconnected silently, DOM node count 493→505 only from real new gate-check/gate stage nodes, focus on the `dispatcher` stage link and scroll retained; `watch-mode-desktop.webp`).
